export interface IThemeInterface {
  success: string,
  primary: string,
  white: string,
  boxShadow: string
}

const theme = {
  boxShadow: '0px 0px 43px -10px black',
  primary: '#007bff',
  success: '#28a745',
  white: '#fefefe'
}

export default theme